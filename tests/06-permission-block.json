{
  "_test": "6-block",
  "_hypothesis": "exit(2) in command blocks subsequent hooks in same array",
  "_expected": "IF TRUE: Δ = 0 (agent never runs). IF FALSE: Δ = agent overhead",
  "_critical": "This determines if scheduler pattern is viable",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f /home/corey/hook-lab/results/work-gate ] && exit 0 || exit 2",
            "timeout": 2
          },
          {
            "type": "agent",
            "prompt": "Gate exists. Return {work: true}.",
            "timeout": 30,
            "model": "haiku"
          }
        ]
      }
    ]
  }
}
