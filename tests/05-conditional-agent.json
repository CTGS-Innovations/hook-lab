{
  "_test": "5-conditional",
  "_hypothesis": "Agent can check gate file and exit early if no work",
  "_expected": "Δ = agent overhead (agent still runs, just exits fast)",
  "_note": "Agent runs regardless - tests agent self-gating",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/home/corey/hook-lab/hooks/gate-checker.sh",
            "timeout": 2
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Check if /home/corey/hook-lab/results/work-gate exists. If yes, return {work: true}. If no, return {work: false} immediately.",
            "timeout": 30,
            "model": "haiku"
          }
        ]
      }
    ]
  }
}
